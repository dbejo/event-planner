spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/eventmgrdb
    username: root
    password: P@ssword924
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
